---
bin_dir: /usr/local/bin
home_dir: /home/sscaling
working_dir: "{{ tmp_dir }}"
ansible_ssh_private_key_file: "{{ home_dir }}/.ssh/sts_ed25519"

state: present

aws_region: "{{ aws.aws_region }}"          # aws region for install
cluster_name: "{{ ocp_cluster.name }}"

openshift_version: "4.18"
ocp_patch_version: "20"
...